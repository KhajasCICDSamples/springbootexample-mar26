---
name: build java project

on:
    workflow_dispatch: 

jobs:
    build:
        uses: shaik2025/springbootexample-mar26/.github/workflows/java_maven_build.yaml@main
        with:
            java_version: '21'
            maven_command: 'package'       